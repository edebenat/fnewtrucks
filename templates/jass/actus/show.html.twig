{% extends 'base-jass.html.twig' %}

{% block title %}Actu{% endblock %}

{% block body %}
    <h1>Actu</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ actu.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ actu.title }}</td>
            </tr>
            <tr>
                <th>Abstract</th>
                <td>{{ actu.abstract }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ actu.content }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('jass_actus') }}">back to list</a>

    <a href="{{ path('jass_actus_edit', {'id': actu.id}) }}">edit</a>

    {{ include('jass/actus/_delete_form.html.twig') }}
{% endblock %}
